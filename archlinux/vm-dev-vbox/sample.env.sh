VM_HOST=
VM_PORT=

if [ -z "$VM_PORT" ]; then
    log "Error: VM_PORT var is not defined in env.sh"
    exit 1
fi

if [ -z "$VM_PORT" ]; then
    log "Error: VM_PORT var is not defined in env.sh"
    exit 1
fi
